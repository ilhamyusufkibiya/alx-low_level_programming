#include <stdio.h>
/**
 * main - A program that prints the file name
 * @argc: Argument count
 * @argv: Argument vector
 * Return: o
 */
int main(int argc, char *argv[])
{
	(void) argc;
	printf("%s\n", argv[0]);
	return (0);
}
